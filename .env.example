# 根级 .env 用于跨包共享的默认值。各包优先使用各自目录下的环境文件：
#
# - 桌面端（Vite）：packages/desktop/.env.development | .env.production
#   变量名 VITE_API_BASE（本地默认 http://localhost:3000，生产见该目录 .env.production）
# - 移动端（Expo）：packages/mobile/.env.development | eas.json production.env
#   变量名 EXPO_PUBLIC_API_BASE（本地默认模拟器 10.0.2.2:3000，生产 http://47.103.104.7:3000）
# - 服务端（Node）：packages/server/.env，变量 PORT（本地 3000；生产机器上可为 8080）
