## 整体架构

[Electron 桌面端]
   │ 1. 用户操作（比如同步、保存日记、拉取工作清单）
   ▼
[API Client]
   │ 2. 发 HTTP 请求（例如 /sync/push, /sync/pull）
   ▼
[Node.js 运行的 Server 项目]
   │ 3. 路由层接收请求（如 routes/sync.ts）
   │ 4. 做鉴权/参数校验/业务规则
   │ 5. 调用 Prisma Client
   ▼
[Prisma]
   │ 6. 把 JS/TS 的查询调用转成 SQL
   ▼
[PostgreSQL（通常跑在 Docker 容器里）]
   │ 7. 执行 SQL，返回结果
   ▲
   └──────── 8. 原路返回给前端，界面更新

Prisma干的两件大事
Schema 建模：在 schema.prisma 定义数据模型（表结构）
Client 调用：在代码里写 prisma.user.findMany() 这类 JS/TS API，而不是手写大量 SQL



## package.json 命令详解
### 命令含义
6. lint : 运行代码 lint 检查
   
   - 执行 pnpm -r --if-present run lint
   - 在所有包中运行 lint 检查（如果存在 lint 脚本）
7. typecheck : 运行类型检查
   
   - 执行 pnpm -r --if-present run typecheck
   - 在所有包中运行类型检查（如果存在 typecheck 脚本）
8. test : 运行测试
   
   - 执行 pnpm -r --if-present run test
   - 在所有包中运行测试（如果存在 test 脚本）
### --filter 参数解释
--filter 是 pnpm 包管理器的一个重要参数，用于在 monorepo 项目 中指定要操作的具体包。

- 作用 ：限制命令只作用于特定的包，而不是整个项目
- 使用场景 ：当项目包含多个子包时，可以只对需要的包执行命令，提高效率
- 语法 ： pnpm --filter <package-name> <command>
### @time-manger 前缀解释
@time-manger 是一个 npm 作用域（scope） ，用于组织相关的包。

## 桌面端
http://localhost:4567/

## 服务端
http://localhost:3000
expo go 连接时不能使用，它会指向手机的本地，应该使用http://192.168.1.28:3000

## 数据库
1.查看数据库
pnpm --filter @time-manger/server exec prisma studio
2.pgSQL不是跑在本机上，而是跑在docker上。docker desktop就是容器运行平台。
数据库本身是容器，所以pgSQL是容器。
3.docker
启动和管理容器（比如 postgres 容器）
提供网络映射（把容器里的 5432 端口映射到你本机）
提供数据卷（数据库文件持久化）
让 docker compose up 这类命令真正运行起来


## 移动端
exp://192.168.1.28:8081

邮箱：test@example.com
密码：password-ok-1
