## 整体架构

[Electron 桌面端]
   │ 1. 用户操作（比如同步、保存日记、拉取工作清单）
   ▼
[API Client]
   │ 2. 发 HTTP 请求（例如 /sync/push, /sync/pull）
   ▼
[Node.js 运行的 Server 项目]
   │ 3. 路由层接收请求（如 routes/sync.ts）
   │ 4. 做鉴权/参数校验/业务规则
   │ 5. 调用 Prisma Client
   ▼
[Prisma]
   │ 6. 把 JS/TS 的查询调用转成 SQL
   ▼
[PostgreSQL（通常跑在 Docker 容器里）]
   │ 7. 执行 SQL，返回结果
   ▲
   └──────── 8. 原路返回给前端，界面更新

Prisma干的两件大事
Schema 建模：在 schema.prisma 定义数据模型（表结构）
Client 调用：在代码里写 prisma.user.findMany() 这类 JS/TS API，而不是手写大量 SQL



## package.json 命令详解
### 命令含义
6. lint : 运行代码 lint 检查
   
   - 执行 pnpm -r --if-present run lint
   - 在所有包中运行 lint 检查（如果存在 lint 脚本）
7. typecheck : 运行类型检查
   
   - 执行 pnpm -r --if-present run typecheck
   - 在所有包中运行类型检查（如果存在 typecheck 脚本）
8. test : 运行测试
   
   - 执行 pnpm -r --if-present run test
   - 在所有包中运行测试（如果存在 test 脚本）
### --filter 参数解释
--filter 是 pnpm 包管理器的一个重要参数，用于在 monorepo 项目 中指定要操作的具体包。

- 作用 ：限制命令只作用于特定的包，而不是整个项目
- 使用场景 ：当项目包含多个子包时，可以只对需要的包执行命令，提高效率
- 语法 ： pnpm --filter <package-name> <command>
### @time-manger 前缀解释
@time-manger 是一个 npm 作用域（scope） ，用于组织相关的包。

## 桌面端
http://localhost:4567/

## 服务端
http://localhost:3000
expo go 连接时不能使用，它会指向手机的本地，应该使用http://192.168.1.28:3000

## 数据库
1.查看数据库
pnpm dlx prisma@5.22.0 studio --schema packages/server/prisma/schema.prisma
2.pgSQL不是跑在本机上，而是跑在docker上。docker desktop就是容器运行平台。
数据库本身是容器，所以pgSQL是容器。
3.docker
启动和管理容器（比如 postgres 容器）
提供网络映射（把容器里的 5432 端口映射到你本机）
提供数据卷（数据库文件持久化）
让 docker compose up 这类命令真正运行起来
4.项目中用 Docker 按 compose 文件帮你装并启动了一个 Postgres 容器，image: postgres:16指定镜像，docker会从网上拉这个镜像，起一个独立容器，跑真正的Postgres服务。

5.Docker Desktop 与 compose 对照表（新手速查）

| 你在项目里写的 compose | Docker Desktop 里看到的东西 | 含义 |
|---|---|---|
| `services.postgres` | Containers 里的 `...postgres...` 容器（Running/Stopped） | 真正跑 PostgreSQL 的容器 |
| `image: postgres:16` | Images 里的 `postgres:16` | Postgres 软件镜像，容器由它创建 |
| `ports: "5432:5432"` | 容器详情里的 Port 映射 | 容器 5432 暴露到本机 `localhost:5432` |
| `volumes: postgres_data:/var/lib/postgresql/data` | Volumes 里的 `postgres_data` | 数据持久化位置，重建容器后数据通常还在 |
| `environment: POSTGRES_USER/POSTGRES_PASSWORD/POSTGRES_DB` | 容器启动时注入环境变量（桌面端可在容器详情看到） | 初始化数据库用户、密码和默认库 |
| `services.server` | Containers 里的 `...server...` 容器（如果你起了它） | 后端也可以容器化运行；本地 `pnpm run server:dev` 则不依赖这个容器 |
| `DATABASE_URL=...@postgres:5432/...`（server 容器内） | 同一 Compose 网络内服务名互通 | 容器内用 `postgres` 主机名；本机启动 server 时要用 `localhost` |

常用操作：
- 只启动数据库：`pnpm run db:up`（等价于 `docker compose -f packages/server/docker-compose.yml up -d postgres`）
- 看容器是否正常：打开 Docker Desktop -> Containers，看 postgres 是否 Running
- 看表和数据：推荐 `pnpm --filter @time-manger/server exec prisma studio` 或使用 DBeaver/pgAdmin 连 `localhost:5432`


## 移动端
exp://192.168.1.16:8081

网址：http://192.168.1.16:3000
邮箱：test@example.com
密码：password-ok-1

## shared

就相当于一个本地随时修改的依赖包，但是别的项目中是打包后的产物，所以当里面的代码更新的时候，需要随时打包
来更新依赖包

别的包（server、desktop）运行时 import 的是「已经编译好的 dist」，

## linux命令
切换成管理员  sudo su -
管理员权限登录  sudo -i
我是谁     whoami
查看docker配置文件   cat /etc/docker/daemon.json
重启docker  sudo systemctl restart docker
查看特定url发请求，响应体输出到终端  curl  curl -v https://registry-1.docker.io/v2/



echo '{"registry-mirrors": ["https://m26vfbfk.mirror.aliyuncs.com"]}' | sudo tee /etc/docker/daemon.json

echo 将后面的字符串覆盖原来的字符串 | 把前面的操作交给后面的操作
tee 覆盖写入

## docker 命令
创建名为timemanger-net的网络   docker network create timemanger-net
在docker仓库拉取镜像  docker pull registry.cn-hangzhou.aliyuncs.com/library/postgres:16
docker pull postgres:16

方案 2：离线导入镜像
如果代理也无法解决问题，可以通过离线方式将镜像导入到服务器。

第一步：在可联网的机器上保存镜像
在一台可以正常访问Docker Hub的机器上运行以下命令：

docker pull postgres:16
docker save -o postgres_16.tar postgres:16
第二步：传输镜像到云服务器
使用SCP或其他工具将生成的postgres_16.tar文件传输到云服务器。例如：

scp postgres_16.tar user@your-server-ip:/path/to/destination/
第三步：加载镜像
在云服务器上加载镜像：
docker load -i /path/to/postgres_16.tar

是否同意执行以下命令并查看输出？

执行
Ctrl↵d

修改
CtrlE

拒绝
Ctrl⌫
docker load -i /path/to/postgres_16.tar
方案 3：更换云服务器的网络环境

scp postgres_16.tar root@47.103.104.7:/home/user/docker-images/

1b2414bcd33a4af9b024c0e50b45cf9d

cd /root/time-manager/packages/server

打印后端服务日志
docker compose logs -f server

通过ssh连接远程服务器
ssh -i "C:\Users\ASUS\.ssh\time.pem" root@47.103.104.7

打开后相当于在轻量服务器开远程连接，用管理员的身份打开终端操作服务器

scp -i "C:\Users\ASUS\.ssh\time.pem" postgres_16.tar root@47.103.104.7:/home/user/docker-images/


把项目文件夹拷到服务器上（用 Git 克隆，或用 WinSCP 把整个 time-manager 文件夹传上去都行）。

在服务器上装好 Docker（你说已经装好了，这步可以跳过）。

在服务器上打开项目里的 packages/server 文件夹，新建一个叫 .env 的文件，里面只写两行「很长的随机密码」，给系统用来签发登录令牌（随便用密码生成器生成两段各 40 位乱码即可）。
变量名需要是：JWT_ACCESS_SECRET= 和 JWT_REFRESH_SECRET=（具体格式你打开仓库里的 .env.example 对照着写也行）。

在服务器上，进入整个项目的最外层文件夹（里面有 package.json、pnpm-workspace.yaml 的那一层），在终端里执行这一条命令（整行复制）：
docker compose -f packages/server/docker-compose.yml up -d --build
它会自动：下载/启动数据库、编译并启动后端。第一次可能要等几分钟。

在阿里云控制台里，给这台机器的安全组放行「3000」端口（不然外面访问不到）。
之后在浏览器里访问：http://47.103.104.7:3000（如果项目有健康检查接口可以试；没有的话用手机 App 里填同一个地址+端口）。